@model EZYSoft2.Models.ResetPasswordViewModel

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/passwordValidation.js"></script> <!-- ✅ External JS File -->
}

<h2>Reset Password</h2>

<form asp-action="ResetPassword" method="post" id="resetPasswordForm">
    @Html.AntiForgeryToken()

    <input type="hidden" asp-for="Token" />
    <input type="hidden" asp-for="Email" />

    <!-- 🔹 New Password with Strength Meter -->
    <div class="form-group">
        <label asp-for="NewPassword"></label>
        <input asp-for="NewPassword" class="form-control" type="password" id="NewPassword" required />
        <small id="passwordRequirementsText" class="form-text text-danger"></small>

        <!-- 🔹 Password Strength Meter -->
        <div id="passwordStrengthMeter">
            <div id="strengthBar"></div>
        </div>

        <span asp-validation-for="NewPassword" class="text-danger"></span>
    </div>

    <style>
        #passwordStrengthMeter {
            width: 100%;
            height: 10px;
            background-color: #ddd;
            margin-top: 5px;
            border-radius: 5px;
        }

        #strengthBar {
            height: 100%;
            width: 0%;
            border-radius: 5px;
        }
    </style>

    <!-- 🔹 Confirm New Password -->
    <div class="form-group">
        <label asp-for="ConfirmPassword"></label>
        <input asp-for="ConfirmPassword" class="form-control" type="password" id="ConfirmPassword" required />
        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Reset Password</button>
</form>
